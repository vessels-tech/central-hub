'use strict'

import React from 'react';
import { Button } from 'semantic-ui-react';

const FilterButton = () => (
  <Button content='Filter' icon='filter' primary />
);

export default FilterButton;
